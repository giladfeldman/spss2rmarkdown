% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_sps.R
\name{build_alias_map}
\alias{build_alias_map}
\title{Build an alias map from a parsed .sps script}
\usage{
build_alias_map(parsed)
}
\arguments{
\item{parsed}{List of parsed commands from \code{\link[=parse_sps]{parse_sps()}}.}
}
\value{
Character vector of all variable names referenced in the script
(uppercased, unique).
}
\description{
SPSS legacy scripts often use 8-character-truncated variable names. The
canonical (full) name is sometimes recoverable from the syntax itself
through:
}
\details{
\itemize{
\item \verb{RECODE old INTO new} — both names appear together,
\item \verb{VARIABLE LABELS varname 'Long form'} — a label hints at the full name
(we record the literal varname rather than guessing),
\item \verb{RENAME VARIABLES (old=new)} — explicit rename pair.
}

We collect every name that appears in the script and return a unique,
upper-cased character vector. Downstream code uses \link{resolve_truncated_name}
to match a referenced name back to a \code{.sav} variable when the script form
is truncated.
}
