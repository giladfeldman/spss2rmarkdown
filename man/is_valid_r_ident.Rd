% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/syntax_converter.R
\name{is_valid_r_ident}
\alias{is_valid_r_ident}
\title{Validate a generated R identifier (cross-project rec R-0009)}
\usage{
is_valid_r_ident(x)
}
\arguments{
\item{x}{A candidate identifier.}
}
\value{
TRUE iff \code{x} is a single non-empty syntactically-valid R name.
}
\description{
Ported verbatim from STATA2Rmarkdown's \code{is_valid_r_ident()}. SPSS variable
names are normalized by \code{\link[=normalize_spss_names]{normalize_spss_names()}} before being interpolated
into generated R as bare identifiers (e.g. the LHS of \code{dplyr::mutate()}).
Almost every non-empty input normalizes to a valid name, but a degenerate
target (an unexpanded macro that collapses to "", a multi-element vector,
etc.) would emit unparseable R like \verb{mutate( = expr)}. Callers gate the
target through this predicate and fall back to a NOTE when it is invalid.
}
\keyword{internal}
