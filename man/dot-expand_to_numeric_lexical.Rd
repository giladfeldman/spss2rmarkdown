% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/syntax_converter.R
\name{.expand_to_numeric_lexical}
\alias{.expand_to_numeric_lexical}
\title{Lexically expand a numeric-sibling SPSS TO range without a SAV dictionary}
\usage{
.expand_to_numeric_lexical(from_var, to_var)
}
\arguments{
\item{from_var}{Left endpoint token (e.g. "item1")}

\item{to_var}{Right endpoint token (e.g. "item10")}
}
\description{
SPSS's \verb{var1 TO varN} most commonly enumerates a family of names that share
a common prefix/suffix and differ only by a trailing integer field
(\verb{item1 TO item10}, \verb{risk01_prep2 TO risk07_prep2}, \verb{Q1a TO Q5a}). When the
.sav dictionary lookup fails — because the items were COMPUTE-created at
runtime (not in any .sav), or the script is paired with multiple .sav and the
primary one doesn't hold these vars — we can still reproduce SPSS's intent by
expanding that integer field lexically. This is correct for the dominant
contiguous-numeric idiom and, crucially, stops the literal keyword \code{'TO'} from
leaking into the generated jmv/dplyr call (which crashes with
\code{"'names' attribute [N] must be the same length as the vector [0]"}).
}
\details{
Returns the expanded character vector (preserving the source token case and
zero-padding width), or \code{NULL} when the two endpoints are not a single
varying integer field (caller then falls back to its own behaviour).
}
\keyword{internal}
