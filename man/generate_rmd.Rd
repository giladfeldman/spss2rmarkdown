% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rmd_generator.R
\name{generate_rmd}
\alias{generate_rmd}
\title{Generate R Markdown document}
\usage{
generate_rmd(
  sav_data,
  sav_path,
  parsed_syntax,
  converted_code,
  output_dir,
  options = list(),
  base_name = NULL,
  conversion_notes = list(),
  secondary_sav_files = character()
)
}
\arguments{
\item{sav_data}{SAV data info from parse_sav()}

\item{sav_path}{Original SAV file path}

\item{parsed_syntax}{Parsed SPSS syntax}

\item{converted_code}{Converted R code blocks}

\item{output_dir}{Output directory}

\item{options}{Rendering options}

\item{base_name}{Optional base name for output files}

\item{conversion_notes}{Optional list of conversion notes/warnings to surface
in the generated document}

\item{secondary_sav_files}{Optional character vector of additional .sav paths
uploaded alongside the primary one. Row-aligned files are merged into
\code{data}; a row-count mismatch is skipped with a note rather than
positionally cbind-ed (which would misalign rows).}
}
\value{
List with rmd_path and data_path
}
\description{
Creates a complete R Markdown document from parsed and converted SPSS
syntax, including data loading, transformations, and analyses.
}
\examples{
\dontrun{
parsed    <- parse_sps("analysis.sps")
sav_info  <- parse_sav("data.sav")
converted <- convert_all_commands(parsed, sav_info)
rmd <- generate_rmd(
  sav_data       = sav_info,
  sav_path       = "data.sav",
  parsed_syntax  = parsed,
  converted_code = converted,
  output_dir     = tempdir()
)
rmd$rmd_path
}
}
